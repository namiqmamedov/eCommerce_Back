@model List<CartVM>

<div class="cart__main">
    <div class="container">
        <div class="row">
            <div id="cartIndex" class="cart__product">
                @await Html.PartialAsync("_CartIndexPartial", Model)
            </div>
        </div>
    </div>
</div>